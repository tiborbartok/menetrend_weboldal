import { DatumFormatumPipe } from './datum-formatum.pipe';

describe('DatumFormatumPipe', () => {
  it('create an instance', () => {
    const pipe = new DatumFormatumPipe();
    expect(pipe).toBeTruthy();
  });
});
